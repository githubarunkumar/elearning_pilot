{
  "name": "acg-elearning-pilot",
  "private": true,
  "version": "1.0.0",
  "description": "ACG Safety & Environmental Awareness e-learning pilot - single-service deployment wrapper. Builds the React client and runs the Express server, which serves the built client and the API from one process.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
